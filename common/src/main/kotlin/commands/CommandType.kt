package request

enum class CommandType {
    Add,
    Clear,
    CountByNumbersOfParticipants,
    Exit,
    FilterByAlbumsCount,
    Help,
    Info,
    RemoveAnyByFrontMan,
    RemoveAt,
    RemoveById,
    Reorder,
    Save,
    Show,
    Shuffle,
    Update
}